{
  "name": "<%= name %>",
  "title": "<%= title %>",
  "description": "<%= description %>",
  "version": "0.0.0",
  "author": {
    "name": "<%= author %>",
    "email": "<%= email %>"
  },
  "bugs": "https://github.com/<%= username %>/<%= name %>/issues",
  "contributors": [
    {
      "name": "<%= author %>",
      "email": "<%= email %>"
    }
  ],
  "dependencies": {
    "@flickmy/bic": "file:../bic",
    "@flickmy/bic-logger": "file:../bic-logger",
    "app-root-path": "^1.0.0",
    "basic-auth": "^1.0.3",
    "compression": "^1.5.2",
    "csso": "^1.3.11",
    "errorhandler": "^1.4.2",
    "express": "^4.13.3",
    "express-cache-response-directive": "^0.2.0",
    "express-conditional-middleware": "^1.0.3",
    "faker": "^3.0.1",
    "fs-extra": "^0.24.0",
    "html-minifier": "^0.7.2",
    "img-placeholder-src": "^0.2.4",
    "jquery": "^2.1.4",
    "lodash": "^3.10.1",
    "morgan": "^1.6.1",
    "normalize.css": "^3.0.3",
    "rekuire": "^0.1.9",
    "uglify-js": "^2.4.24"
  },
  "devDependencies": {
    "blue-tape": "^0.1.10",
    "bump-cli": "^1.1.3",
    "faucet": "0.0.1",
    "fixpack": "^2.2.0"
  },
  "engines": {
    "node": "4.1.x",
    "npm": "2.14.x"
  },
  "homepage": "https://github.com/<%= username %>/<%= name %>#readme",
  "keywords": [
    "bic",
    "demo",
    "framework",
    "website"
  ],
  "license": "MIT",
  "main": "app",
  "repository": "github:<%= username %>/<%= name %>",
  "scripts": {
    "clean": "./bin/clean.sh",
    "commit:pre": "git add -A && git commit -m 'Prerelease' && git push",
    "decrypt": "bic decrypt",
    "encrypt": "bic encrypt",
    "heroku": "foreman start",
    "local": "bic sequence local",
    "logs": "heroku logs --app=<%= name %> --tail",
    "logs:staging": "heroku logs --app=<%= name %>-staging --tail",
    "postinstall": "bower install",
    "preinstall": "npm install -g bower gulp",
    "prestart": "bic sequence deploy --release",
    "publish:pre": "npm publish --access=public",
    "release:major": "fixpack && bump .package.json --major",
    "release:minor": "fixpack && bump .package.json --minor",
    "release:patch": "fixpack && bump .package.json --patch",
    "release:pre": "fixpack && bump .package.json --pre && npm run commit:pre && npm run publish:pre",
    "setup": "./bin/setup.sh",
    "setup:buildpacks": "heroku buildpacks:set <%= buildpack %> --app=<%= name %> && heroku buildpacks:set <%= buildpack %> --app=<%= name %>-staging",
    "start": "node app --release",
    "test": "faucet"
  }
}
