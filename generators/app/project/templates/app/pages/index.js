'use strict';

/* All known, "common" resources should be declared in `app/common/index.js`. */
require('common');

/* Page specific resources should be declared below (if they are "common", webpack will figure it out). */

require('./index.scss');

module.exports = null;
